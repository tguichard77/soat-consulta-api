services:
  - type: web
    name: soat-consulta-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn soat_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health